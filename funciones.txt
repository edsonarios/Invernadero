FUNCIONES EN EL API-REST

1. En la tabla Invernadero, el atributo temperatura maxima, debe ser el promedio de todos los productos instanciados en el Invernadero
2. En la tabla Invernadero, el atributo temperatura minima, debe ser el promedio de todos los productos instanciados en el Invernadero

3. En la tabla historialProducto, copiar los datos de producto, mediante triger, haciendo sumas a la fecha

Instanciar Usuario
1. se deben los datos de la base de datos
    el usuario debe persistir para otros formularios o hacer llamado

    select *
    from usuario u, invernadero i, producto p, hisprod h 
    where u.id = i.usuarioid
    and i.id = p.invernaderoid
    and p.id = h.productoid

MIRE DEFECTOS 
Se tenia un error al conectar back con fronend, Angular con la api
No se tenia acceso a la api de parte de angular, por lo q se necesitaba permiso
Se implemento en el middelware de la api este troso de linea de codigo, para que angular obtuviera permiso a la api

res.header("Access-Control-Allow-Origin", "*");
  res.header("Access-Control-Allow-Headers", "Origin, X-Requested-With, Content-Type, Accept");
--------------------------------------------------------------------------------------------------------------------------------

  

FUNCIONES QUE NECESITA RICHAR EL 23 DE MARZO DE 2018


eliminar controlador con sus respectivos pines = idcontrolador

eliminar invernadero - controlador- pines = idinvernadero 

eliminar usuario --- = idusuario 

eliminar producto de la tabla producto = idproducto 

eliminar producto de la tabla historialProducto = idhistorialproducto 


update todo el usuario = idusuario            /updateUsuario post

update = eliminar un pin = idpin            /desactivarPin    por idpin

update invernadero = idinvernadero          /updateInvernadero   por idinvernadero

post idcontrolador = devolver controlador   /obtenerControlador por id controlador








